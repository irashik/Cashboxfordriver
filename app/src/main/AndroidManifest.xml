<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="irashindmitrii.cashboxfordriver"

    >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_local_printshop_black_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_local_printshop_black_24dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"

        >

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:label="@string/about">
        </activity>

        <activity
            android:name=".HelpActivity"
            android:label="@string/help">
        </activity>



        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="c91256a07a0d98fc44564b16c86a911ca57881f7"
            />


    </application>

</manifest>